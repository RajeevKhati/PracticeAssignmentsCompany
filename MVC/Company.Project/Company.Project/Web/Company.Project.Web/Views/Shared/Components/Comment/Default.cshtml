@model IEnumerable<CommentViewModel>

@if (Model.Count() > 0)
{
    <hr />
    <p class="h4">Comments</p>
}

<ul class="list-group">
    @foreach (var comment in Model)
    {
        <li class="list-group-item mt-3">@comment.Content</li>
    }
</ul>